stages: [pipeline]

prd:
  stage: pipeline
  script:
    - echo ok.
  rules:
    - if: '$CI_COMMIT_REF_NAME == "master"'

stg:
  stage: pipeline
  script:
    - echo ok.
  rules:
    - if: "$CI_COMMIT_TAG || $CI_COMMIT_BRANCH"
