#!/bin/bash
set -euo pipefail; ROOT="$(cd $(dirname $BASH_SOURCE) && cd .. && pwd)"

export PYTHONPATH="${ROOT}/src/"
export FLASK_APP=diaas.dss:app
export FLASK_ENV=${FLASK_ENV:-development}
export FLASK_DEBUG=1

flask "$@"
